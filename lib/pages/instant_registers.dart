import 'package:flutter/material.dart';

class InstantRegisters extends StatelessWidget {
  const InstantRegisters({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Center(child: Text("Instant Register")),
      ),
    );
  }
}
